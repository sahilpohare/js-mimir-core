export default class MimirTask{

    /**
     * @param {import("../rpc").MimirRequest request} -Request
     * @param {import("peer-id")}
     */
    constructor(request, peerID){
        this.functionImage = functionImage;
        this.body = body,
    }
}